Function.prototype.call = function (context) {
  let contet = context | window;
  let fn = Symbol('fn');
  let fn = Symbol('fn');
  contet.fn = this;

  let agrs = [];
  for(let i = 1, len = arguments.length; i++) {
    this.propxythis
    agrs.push();
  }
}